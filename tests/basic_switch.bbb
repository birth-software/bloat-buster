E = enum
{
    a,
    b,
    c,
}

[export] main = fn [cc(c)] () s32
{
    >some_enum: E = .a;
    switch (some_enum)
    {
        .a =>
        {
            return 0;
        },
        .b =>
        {
            return 1;
        },
        .c =>
        {
            return 1;
        },
    }
}
