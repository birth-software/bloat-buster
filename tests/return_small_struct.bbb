S = struct
{
    a: u1,
    b: u1,
}

foo = fn [cc(c)] () S
{
    return { .a = 1, .b = 0 };
}

[export] main = fn [cc(c)] () s32
{
    >s = foo();

    if (s.a != 1) #trap();
    if (s.b != 0) #trap();

    return 0;
}
