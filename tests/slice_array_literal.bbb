foo = fn (slices: [][]u8) void
{
    if (slices.length != 3)
    {
        #trap();
    }
}
[export] main = fn [cc(c)] () s32
{
    >some_bool: u1 = 0;
    foo([ "abc", #select(some_bool, "bcd", "cbd"), "sas", ][..]);
    return 0;
}
